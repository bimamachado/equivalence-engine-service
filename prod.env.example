# Example production environment variables (do NOT commit secrets)
# Fill values from your secret manager or CI/CD pipeline.

# Postgres
POSTGRES_USER=equivalence
POSTGRES_PASSWORD=changeme_here
POSTGRES_DB=equivalence
DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Redis
REDIS_URL=redis://redis:6379/0
RQ_QUEUE_NAME=equivalence

# API keys / salts
# Use a long random salt and store it securely
API_KEY_SALT=$(openssl rand -hex 32)
# Admin/auditor/client keys should be generated securely and provided via secret manager
ADMIN_API_KEY=replace-with-secure-key
AUDITOR_API_KEY=replace-with-secure-key
CLIENT_API_KEY=replace-with-secure-key

# External services (embeddings / LLM)
EMBED_URL=http://mock-embed:9101
LLM_URL=http://mock-llm:9102
LLM_API_KEY=replace-with-llm-key
EMBED_API_KEY=replace-with-embed-key

# App runtime
# e.g. uvicorn host/port, log level, etc (optional override)
APP_HOST=0.0.0.0
APP_PORT=8100
LOG_LEVEL=info

# Notes:
# - Never commit a real prod.env to git. Keep values in a secrets manager and inject at deploy time.
# - For Kubernetes, map these to Secrets and mount as envFrom.
